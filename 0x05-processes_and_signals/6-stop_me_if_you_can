#!/usr/bin/env bash
# Stops only the 4-to_infinity_and_beyond script processes silently

for PID in $(pgrep -f 4-to_infinity_and_beyond); do
    if [ "$PID" != "$$" ]; then
        pkill -TERM -P "$PID"
    fi
done
